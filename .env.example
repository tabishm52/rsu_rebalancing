# Optional: point this at a directory to enable on-disk caching of fetched prices.
# If unset, prices are fetched live and memoized in-memory for the session only.
# Copy this file to `.env` and uncomment to enable.
#
# RSU_REBALANCING_CACHE_DIR=./cache
